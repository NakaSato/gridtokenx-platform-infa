apiVersion: v1
kind: Service
metadata:
  name: explorer
  namespace: gridtokenx
  labels:
    app: explorer
    component: frontend
spec:
  type: ClusterIP
  ports:
  - port: 4000
    targetPort: 4000
    protocol: TCP
    name: http
  selector:
    app: explorer
