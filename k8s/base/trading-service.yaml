apiVersion: v1
kind: Service
metadata:
  name: trading
  namespace: gridtokenx
  labels:
    app: trading
    component: frontend
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: trading
